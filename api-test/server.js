const express = require('express'); const app = express(); const PORT = 3000; app.get('/', (req, res) => { console.log('Root path accessed with query:', req.query); const logs = [{ timestamp: new Date().toISOString(), level: 'INFO', message: 'Test log entry 1' }, { timestamp: new Date().toISOString(), level: 'DEBUG', message: 'Test log entry 2' }]; res.json({ status: 'success', data: { logs: logs, count: logs.length } }); }); app.get('/api/v1/logs', (req, res) => { res.json({ status: 'success', data: { logs: [{ timestamp: new Date().toISOString(), level: 'INFO', message: 'Test log entry' }], count: 1 } }); }); app.get('/api/v1/system/logs', (req, res) => { res.json({ status: 'success', data: { logs: [{ timestamp: new Date().toISOString(), level: 'INFO', message: 'Test log entry' }], count: 1 } }); }); app.get('/api/v1/health', (req, res) => { res.json({ status: 'success', message: 'API is healthy' }); }); app.listen(PORT, () => { console.log('Test API server listening on port', PORT); });
