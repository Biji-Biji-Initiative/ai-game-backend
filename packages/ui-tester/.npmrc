# Prevent nested node_modules installations
node-linker=hoisted
# Use workspace protocol for better monorepo management
public-hoist-pattern=*
# Respect workspace dependencies
link-workspace-packages=true
